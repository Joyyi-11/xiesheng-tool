# 可选：自动化校订的 LLM API（默认不配置，走免费会话内处理）
# 如需自动化后处理，在操作系统用户环境变量中同时设置：
#   LLM_API_KEY  -> 你的 API Key
#   LLM_BASE_URL -> OpenAI 兼容接口地址（如 https://your-provider/v1）
# 未设置时，工具自动进入 --no-llm 会话内链路，产物可直接粘贴到 AI 会话做后处理。
# LLM_API_KEY=your_api_key
# LLM_BASE_URL=https://your-openai-compatible-endpoint/v1