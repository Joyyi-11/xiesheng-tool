# 贡献指南

撷声把小宇宙播客链接到结构化 Markdown 文稿的整条链路放在本地跑：抓取元信息 → 本地 ASR 转写 → 说话人区分 → LLM 校订与提炼。感谢你愿意参与。

## 先读这一节：仓库边界

这个公开仓库是**人工策展的发布面**，不是某个私有开发仓的镜像。`src/`、`tests/`、`README.md`、`pyproject.toml`、`requirements.txt` 由维护者按需同步而来，同步动作由人工判断、不是逐字拷贝。

由此有两条实际影响：

- **代码类改动可以直接提 PR**：ASR 后端、说话人区分、渲染、缓存、CLI、文档。
- **校订口径与输出结构的改动，请先开 issue 讨论**：这两处是跨链路共享的约定（见下一节），改动要同时落到私有源与公开版；直接提 PR 会让两边分叉，下一次同步时互相覆盖。

## 两条链路，一套口径

项目支持两种 LLM 后处理方式：

| 链路 | 入口 | 形态 |
|---|---|---|
| api | `--llm-mode api` | 分块调用 LLM API，逐块校订后再一次性结构化 |
| session | `--llm-mode session` | 在会话窗口内由 agent 完成校订与结构化 |

**流程层可以不同，规则层必须同源。** 全部校订口径（`EDIT_*`）与结构化口径（`STRUCT_*`）集中在 `src/processor/rules.py`，两条链路引用同一批常量；`RULES_SPEC_VERSION` 是共享版本号，**任何条款文字变更都要递增它**，两条链路的提示词会随常量自动引用新版本号。

改动口径时：

1. **只改 `src/processor/rules.py`**，不要在 `src/processor/prompt.py` 或 `src/processor/session_edit.py` 里另写一份条款文本——这两处历史上曾各维护一份、发生漂移，`tests/test_rules_single_source.py` 就是为守住这条线而存在的。
2. **递增 `RULES_SPEC_VERSION`**（同一批常量被两条链路共用，版本号是「改口径必两处同版本」的凭据）。
3. **结构改动是三处一致的**：规范（`rules.py`）↔ 校验器（`src/processor/session_edit.py` 的 `validate_session_output`）↔ 渲染器（`src/renderer/markdown.py`）。只改规范不改校验器，新格式的产物会被自己的校验器拦下。

## 常见的贡献类型

### 转录质量问题

这类问题在本地 ASR 与 LLM 校订之间分层，报之前先判一层，能省很多来回：

| 现象 | 更可能在 |
|---|---|
| 人名 / 产品名 / 机构名被识别成别的字或字母（如英文品牌被音译成一串汉字） | 本地 ASR（未登录词） |
| 同音错字（普通词汇听错） | 本地 ASR，通常可在校订层修 |
| 半角标点、重复字、断句错误 | 校订层 |
| 说话人标签贴错人、多段被并成一段 | 说话人区分 |
| 内容缺失、被改写了原意 | 校订层 |

改这类问题时请**附上实际跑过的一期节目与逐字片段**。本项目对「感觉更准了」不敏感，对可复核的对照敏感——同一段音频可复核，才能确认改动是修好而不是另一种错。

### 新增 ASR 或说话人后端

`src/transcriber/` 与 `src/diarization/` 按后端分文件。新增后端请说明：模型体积、CPU 上的实测速度、以及它与现有后端在**同一段音频**上的差异。只在更快或更准其中一项占优时也值得提交，但请写清是哪一项。

### 文档与说明

README 里对速度、成本、耗时的描述都来自实测；修改这类数字请说明测法与机器环境。

## 提交之前

```bash
pip install -e ".[dev]"

# 1. 单元测试：全量应全绿
pytest -q

# 2. 结构校验：对一份本地已生成的成稿跑，检查板块顺序、说话人标签、
#    自称一致性、转录区回标加粗等。只做检查、不调用 LLM。
python -m src.processor.session_edit --check output/<节目名>.md

# 3. 代码风格
ruff check .
```

第 2 步的成稿路径指向你本地 `output/` 下的产物（该目录不进版本库）。校验未通过时会打印具体条目，逐条看即可。

## 语言与约定

- 中文、英文都可以。
- 讨论对事不对人；不确定的改动先开 issue 问一句，比提一个大 PR 更省双方时间。
